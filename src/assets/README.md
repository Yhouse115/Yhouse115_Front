# Assets

Place committed frontend static assets here only when the application actually uses them.

Do not commit raw source datasets, local exports, temporary downloads, or generated build artifacts.
